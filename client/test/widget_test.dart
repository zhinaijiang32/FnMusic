import 'package:flutter_test/flutter_test.dart';
import 'package:tunecache/app.dart';

void main() {
  test('app widget is constructible', () {
    expect(const TuneCacheApp(), isA<TuneCacheApp>());
  });
}
